# Get a key at https://platform.openai.com/api-keys
# GPT-5 + reasoning summaries + hosted web search all require an account
# with access to the Responses API (standard for any modern OpenAI key).
OPENAI_API_KEY=sk-...
